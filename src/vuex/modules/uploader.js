import config from '@/config'
import * as types from '@/vuex/mutation-types'

const state = {
	url: config.apiUrl + '/uploader/image'
}

const getters = {
}

const actions = {
}

const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}